<div class="cform-field uk-width-1-{GRID}" >
        <input type="number" id="{ID}" name="{FIELD}" class="uk-input {CLASS}" placeholder="{PLACEHOLDER}" style="{STYLE}" value="{VALUE}" {REQUIRED}>
        <label for="{ID}" class="uk-form-label">{LABEL}</label>
</div>
