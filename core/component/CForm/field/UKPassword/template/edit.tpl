<div class="cform-field uk-width-{GRID}@m">
        <input type="password" pattern=".*" id="{ID}" name="{FIELD}" class="uk-input {CLASS}" placeholder="{PLACEHOLDER}" style="{STYLE}" value="" {READONLY} required>
        <label for="{ID}" class="uk-form-label">{LABEL} <span>{REQUIRED}</span></label>
</div>
